name: Update Index and Deploy Pages

on:
  push:
    branches:
      - main
    paths:
      - 'public/voices/**'
  workflow_dispatch:

# GitHub Pagesへのデプロイに必要な権限設定
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  update_pages:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # v3 -> v4

      - name: Set up Python
        uses: actions/setup-python@v5 # v4 -> v5
        with:
          python-version: 3.11

      - name: Install dependencies
        run: pip install -r src/requirements.txt

      - name: Generate index.html
        run: python src/generate_index.py

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5 # v3 -> v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3 # v1 -> v3

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4 # v1 -> v4
